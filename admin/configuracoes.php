<?php
require_once __DIR__ . '/../includes/helpers.php'; require_auth(); $pdo=db(); $user=admin_user(); $msg='';$err='';
if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$current=(string)$_POST['current_password'];$new=(string)$_POST['new_password'];$confirm=(string)$_POST['confirm_password'];$stmt=$pdo->prepare('SELECT password_hash FROM users WHERE id=?');$stmt->execute([$user['id']]);$hash=$stmt->fetchColumn(); if(!password_verify($current,(string)$hash)){$err='Senha atual incorreta.';}elseif(strlen($new)<10){$err='Use pelo menos 10 caracteres.';}elseif($new!==$confirm){$err='A confirmação não confere.';}else{$pdo->prepare('UPDATE users SET password_hash=? WHERE id=?')->execute([password_hash($new,PASSWORD_DEFAULT),$user['id']]);$msg='Senha alterada com sucesso.';}}
include __DIR__.'/../includes/admin_header.php';
?>
<div class="admin-content"><div class="page-head"><div><h1>Configurações</h1><p>Segurança do acesso administrativo.</p></div></div><?php if($msg):?><div class="alert"><?=e($msg)?></div><?php endif;?><?php if($err):?><div class="alert error"><?=e($err)?></div><?php endif;?><section class="panel"><h2>Alterar senha</h2><form method="post" class="form-grid"><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><label>Senha atual<input type="password" name="current_password" required></label><div></div><label>Nova senha<input type="password" name="new_password" required></label><label>Confirmar nova senha<input type="password" name="confirm_password" required></label><div class="full"><button class="btn btn-primary">Salvar nova senha</button></div></form></section></div>
<?php include __DIR__.'/../includes/admin_footer.php';?>
